# Fulfillment Distress Analysis — Python dependencies
#
# Install with:
#   pip install -r requirements.txt

pandas>=2.0.0
openpyxl>=3.1.0   # Excel read/write engine (used by pandas for .xlsx files)
